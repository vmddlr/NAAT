<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="CLARO"
        android:textSize="@dimen/txt_eighteen"
        android:textColor="@color/white"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/tvShow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/str_show"
        android:textColor="@color/white"
        android:textSize="@dimen/txt_fifteen"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tvTitle"
        app:layout_constraintBottom_toBottomOf="@+id/tvTitle"/>

    <ImageButton
        android:id="@+id/ibCompany"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_claro"
        android:backgroundTint="@color/blue_background"
        android:importantForAccessibility="no"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tvTitle" />

    <TextView
        android:id="@+id/tvDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dimension_five"
        android:gravity="center"
        android:textSize="@dimen/txt_eighteen"
        android:textStyle="bold"
        android:textColor="@color/white"
        tools:text="Tiempo aire"
        android:paddingBottom="@dimen/dimension_ten"
        app:layout_constraintStart_toStartOf="@+id/ibCompany"
        app:layout_constraintEnd_toEndOf="@+id/ibCompany"
        app:layout_constraintTop_toBottomOf="@+id/ibCompany" />

</androidx.constraintlayout.widget.ConstraintLayout>